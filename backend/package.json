{
  "name": "coachai-backend",
  "version": "2.0.0",
  "description": "CoachAI serverless backend — RAG with OpenRouter embeddings + pgvector-ready Postgres",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node index.js",
    "start": "node index.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
