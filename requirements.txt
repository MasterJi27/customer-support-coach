# Development of AI-Powered Customer Support Assistant with Live Response Guidance — runtime dependencies
# Core UI
streamlit>=1.28.0

# Data models & settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM provider (Groq)
groq>=0.11.0

# Real tool execution (Jira / Gmail / Slack via Composio v3)
composio>=0.18.1

# ASGI server (Azure App Service / production)
gunicorn>=21.0.0
uvicorn>=0.23.0

# Charts & dataframes
plotly>=5.15.0
pandas>=2.0.0

# Knowledge-base document ingestion (optional formats)
PyPDF2>=3.0.0
python-docx>=1.0.0

# Optional: text-to-speech playback of customer messages & PDF exports
gTTS>=2.5.0
reportlab>=4.0.0
